<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand nav-logo" href=<%= root_url %> >ER Moonlighter Scheduler <%= fa_icon "heartbeat 2x", class:"icon-middle"%> </a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
        <% if current_user.registration_complete %>
        <li class="inactive"><a href="<%= create_email_path %>"><%= fa_icon "envelope-o 2x", class:"icon-middle"%>Send Email </a></li>
        <% if current_user.is_admin? %>
        <li class="inactive"><a href="<%= pending_users_path %>"><%= fa_icon "user-plus 2x", class:"icon-middle"%>Pending Users</a><li>
        <% end %>
        <% if current_user.type != nil %>
        <li class="inactive" id='contacts'><a href="<%= contact_list_path %>"><%= fa_icon "list 2x", class:"icon-middle"%>Contact List</a></li>
        <% end %>
        <% end %>
        <%end %>
      </ul>
      
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li><a href=<%= destroy_user_session_path%> data-method="delete">Logout <%= fa_icon "mail-forward 2x", class:"icon-middle" %></a></li>
        <% else %>
        <li><%= link_to('Login', new_user_session_path)  %></li>
        <% end %>
        <li><a href="/help">Settings <%= fa_icon "gear 2x", class:"icon-middle"%></a>
      </ul>
    </div>
  </div>
</nav>